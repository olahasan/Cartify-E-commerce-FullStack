import LoginForm from "../Components/LoginForm";

const LoginPage = () => {
  return (
    <div style={{ minHeight: "100vh" }}>
      <h1>User Login</h1>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
